{% extends 'hierarchy/base.html.twig' %}

{% block html_title -%}
    Diagnosis  - {{ parent() }}
{%- endblock %}

{% block area_main %}

{% include 'hierarchy/partials/breadcrump.html.twig' with {hierarchy: hierarchy,trailing: 'Diagnosis'} only %}

<h1>Hierarchy Diagnosis</h1>

<div class="details-summary">
<form style="margin-left: auto; "action="{{ path('repair', {hierarchySlug: hierarchy.slug}) }}" method="post" accept-charset="utf-8">
    <button>Repair all</button>
</form>
</div>


{% for d in diagnosis %}
    <details class="details">
        <summary class="{{ html_classes('details-summary', {'color-valid': d.valid, 'color-invalid': not d.valid}) }}">
        {{ d.keyId }}


        <form style="margin-left: auto;" action="{{ path('repair_key',{hierarchySlug: hierarchy.slug, keyId: d.keyId}) }}" method="post" accept-charset="utf-8">
            <button>Repair {{d.keyId}} closure</button>
        </form>

        </summary>

        <div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(25em, 1fr));align-content: stretch;">
        {% for type in d.types %}
            <div>
                <h3 class="{{ html_classes({'color-valid': d.validType(type), 'color-invalid': not d.validType(type)}) }}">{{ type }}</h3>
                <table class="data-table">
                    <thead class="data-table-head">
                        <tr class="data-table-head-row">
                            {% for col in d.columns(type) %}
                            <th class="data-table-head-cell">{{col}}</th>
                            {% endfor %}
                        </tr>
                    </thead>
                    <tbody class="data-table-body">
                        {% for row in d.rows(type) %}
                            <tr class="data-table-row">
                                {% for col in d.columns(type) %}
                                <td class="data-table-cell" align="center">{{ row[col] }}</td>
                                {% endfor %}
                            </tr>
                        {% else %}
                            <tr class="data-table-empty-row">
                                <td class="data-table-empty-cell" colspan="{{d.columns(type)|length}}">✅ Valid</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        {% endfor %}
        
        </div>
    </details>
{% endfor %}


</div>
{% endblock %}