{% block field %}
    <input type="hidden" name="field[{{field.id}}][key]" value="{{field.key.id}}">
    <input class="form-field" type="text" id="field_{{field.id}}" name="field[{{field.id}}][id]" value="{{node is defined ? field.readValueOf(node).id : ''}}">
{% endblock %}

{% block cell %}
    {% set val = field.readValueOf(nodeCollection.getNode(nodeId)) %}
    {% if val %}
        #<a href="{{ path('show_node', {key: val.key, id:val.id}) }}">{{val.id}}</a>
    {% endif %}
{% endblock %}

{% block display %}
    {% set val = field.readValueOf(node) %}
    {% if val %}
        #<a href="{{ path('show_node', {key: val.key, id:val.id}) }}">{{val.id}}</a>
    {% endif %}
{% endblock %}