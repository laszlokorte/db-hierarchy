{% block cell %}
    {% if fieldValue %}
    <span class="badge" style="background-color: #eee;color:#000">
    <svg class="inline-icon" viewBox="0 0 16 16" fill="currentColor">
        <use href="#{{ fieldValue }}-16" />
    </svg>
    {{ fieldValue }}</span>
    {% endif %}
{% endblock %}

{% block display %}  
    {% if fieldValue %}
     <span class="badge" style="background-color: #eee;color:#000">
     <svg class="inline-icon" viewBox="0 0 16 16" fill="currentColor">
        <use href="#{{ fieldValue }}-16" />
    </svg>
    {{ fieldValue }}</span>
    {% endif %}
{% endblock %}
