{% block cell %}
    {{fieldValue ? fieldValue.format('Y-m-d H:i:s') : fieldValue}}
{% endblock %}

{% block display %}
    {{fieldValue ? fieldValue.format('Y-m-d H:i:s') : fieldValue}}
{% endblock %}
