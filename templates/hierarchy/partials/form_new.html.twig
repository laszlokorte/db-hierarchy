
{{ form_start(creationForm) }}

{{ form_errors(creationForm) }}

<dl class="form-container">
   {% if creationForm._nesting is defined %}
      <dt class="form-container-label">{{ form_label(creationForm._nesting) }}</dt>
       <dd class="form-container-field">
       {{ form_widget(creationForm._nesting) }}
       {{ form_errors(creationForm._nesting) }}
       </dd>
       {% if creationForm._nesting.vars.help is not empty %}
       <dd class="form-container-info">
       <i class="icon-link info tooltipped" aria-label="{{creationForm._nesting.vars.help}}" target="_blank">{% include 'hierarchy/partials/icon.html.twig' with {icon: 'question'} only %}</i>
       </dd>
       {% endif %}
   {% endif %}

   {% for field in creationForm.fields %}
       <dt class="form-container-label">{{ form_label(field) }}</dt>
       <dd class="form-container-field">
       {{ form_widget(field) }}
       {{ form_errors(field) }}
       </dd>
       {% if field.vars.help is not empty %}
       <dd class="form-container-info">
       <i class="icon-link info tooltipped" aria-label="{{field.vars.help}}" target="_blank">{% include 'hierarchy/partials/icon.html.twig' with {icon: 'question'} only %}</i>
       </dd>
       {% endif %}
   {% endfor %}
   <dt class="form-container-label form-container-spacer"></dt>
   <dd class="form-container-buttons">
      {% for field in creationForm.buttons %}
          {{ form_widget(field) }}
      {% endfor %}
      
		{% if cancel is defined %}
			{% if cancel is same as(true) %}
				<button class="link-button" type="reset">Reset</button>
			{% else %}
				<a class="link-button" href="{{ cancel }}" title="">Cancel</a>
			{% endif %}
		{% endif %}
		{% if cancel_tab is defined %}
			<label class="link-button" for="{{ cancel_tab }}" title="">Cancel</label>
		{% endif %}
   </dd>
</dl>

{{ form_end(creationForm) }}