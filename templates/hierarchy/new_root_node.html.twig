{% extends 'hierarchy/base.html.twig' %}

{% block html_title -%}
    New {{key|capitalize}} - {{ parent() }}
{%- endblock %}

{% block area_main %}

/ <a href="{{ path('hierarchy_root') }}" title="">Root</a> / <a href="{{ path('list_root_nodes',{key}) }}" title="">{{key|capitalize}}s</a> / New

<h1>New {{key|capitalize}}</h1>

{% include 'hierarchy/partials/form_new.html.twig' with {fields, key} only %}

{% endblock %}