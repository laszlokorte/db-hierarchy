{% extends 'hierarchy/base.html.twig' %}

{% block html_title -%}
    {{ key|capitalize }} {{id}} - {{ parent() }}
{%- endblock %}

{% block area_main %}

{% include 'hierarchy/partials/breadcrump.html.twig' with {parentGroups: node.parents, key, id, trailing: 'edit'} only %}

<h1>Edit {{ key|capitalize }} {{id}}</h1>

{% include 'hierarchy/partials/form_edit.html.twig' with {fields, key, id, values: node.self, then: 'edit'} only %}

{% endblock %}