{% extends 'hierarchy/base.html.twig' %}

{% block html_title -%}
    {{ key.label.singular }} {{id}} - {{ parent() }}
{%- endblock %}

{% block area_main %}

{% include 'hierarchy/partials/breadcrump_.html.twig' with {parentGroups: node.parents, key, id, trailing: 'edit'} only %}

<h1>Edit {{ key.label.singular }} {{id}}</h1>

{% include 'hierarchy/partials/form_edit_.html.twig' with {key, id, values: node.self, then: 'edit'} only %}

{% endblock %}