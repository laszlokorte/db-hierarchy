{% extends 'hierarchy/base.html.twig' %}

{% block html_title -%}
    {{ key.label.singular }} {{node.self.id}} - {{ parent() }}
{%- endblock %}

{% block area_main %}

{% include 'hierarchy/partials/breadcrump.html.twig' with {parentGroups: node.parents, key, id:node.self.id, trailing: 'edit'} only %}

<h1>Edit {{ key.label.singular }} {{node.self.id}}</h1>

{% include 'hierarchy/partials/form_edit.html.twig' with {key, id:node.self.id, values: node.self, then: 'edit'} only %}

{% endblock %}