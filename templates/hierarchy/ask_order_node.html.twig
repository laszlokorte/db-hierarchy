{% extends 'hierarchy/base.html.twig' %}

{% block html_title -%}
    Reorder {{ key.label.singular }} {{node.id}} - {{ parent() }}
{%- endblock %}

{% block area_main %}


{% include 'hierarchy/partials/breadcrump.html.twig' with {nodeParents, trailing: 'move'} only %}

<h1>{{ key.label.singular }} {{node.id}}</h1>

{% include 'hierarchy/partials/tabs.html.twig' with {key, node, active: 'order'} only %}

<h2>Reorder</h2>

{% include 'hierarchy/partials/ordering.html.twig' with {key:key, node, orderTargets} only %}

{% endblock %}